{
  "name": "tracker-bot-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:server": "npm --workspace apps/server run dev",
    "build": "npm --workspace apps/server run build",
    "typecheck": "npm --workspace apps/server run typecheck"
  }
}

