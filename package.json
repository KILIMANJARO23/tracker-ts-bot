{
  "name": "tracker-bot-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:server": "npm --workspace apps/server run dev",
    "dev:miniapp": "npm --workspace apps/miniapp run dev",
    "build": "npm --workspace apps/server run build && npm --workspace apps/miniapp run build",
    "typecheck": "npm --workspace apps/server run typecheck && npm --workspace apps/miniapp run typecheck"
  }
}

